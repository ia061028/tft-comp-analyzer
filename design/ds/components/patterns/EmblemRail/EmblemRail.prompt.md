# EmblemRail

紋章の選択面。デスクトップは 220px のレール、モバイルは同じ中身をボトムシート（`.sheet`）に入れる。

- カテゴリ区切り（`.wall__sep`）は**テキスト見出しを持たない**。素材アイコン + ヘアラインだけで語る。
- 選択状態は金（`tile--on` + `tile__count`）。**金はここにしか出ない**（＝「手の内」）。
- データ上活用できない枚数を選ぶと `tile--over`（灯の赤）で警告する。ブロックはしない。
- 紋章は 19 種で増減しない。1クリックで切り替えられる速度が最優先。

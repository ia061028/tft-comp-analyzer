# Toolbar

**ゲーム中に触るものだけを表に出す** —— ユニット数と並び替え。それ以外（採用数下限 / 生涯ブロンズ /
言語 / Set・Patch・マッチ数・生成時刻）は歯車（`.pop`）に畳む。

クロームの縦占有は 48px。**これ以上増やさない**（縦は全部カードに渡す）。
モバイルではセグメントだけが横スクロールする（`.bar__scroll`）。歯車を同じスクロール領域に入れると
画面外へ押し出されて設定に手が届かなくなる。
